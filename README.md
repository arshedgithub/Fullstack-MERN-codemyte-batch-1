## Day 3 - Express.js Basics

### 1. install dependencies

type below command in terminal to install all relevant dependencies of the project

`npm install` or `npm i`

### 6. start the server

`node app.js` for start the server and `nodemon app.js` for start the server in development mode.